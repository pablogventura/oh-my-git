title = Ignorar los archivos

[description]

Ese pollo corre mucho y cambia a menudo. No queremos tenerlo en nuestros commits.

Añádelo al archivo .gitignore, y prueba a utilizar `git add .`.

[setup]

touch .gitignore
echo important > important
git add important
git commit -m "Commit inicial"

[actions]

echo "$RANDOM" > chicken
